# Outputs

This folder stores channel-specific exports.

- `kindle/`: files ready to send to Kindle, currently HTML or Markdown.
- `notion/`: Markdown pages ready to import or later publish through the Notion API.
- `web/`: optional web-readable exports.

The canonical source should remain in `reports/source/`.

