# Connectors

This folder describes source connectors for the research agent.

The current repo does not require any paid API. Connector configs define optional integrations that can be added later.

Use `configs/source_connectors.json` as the registry.

