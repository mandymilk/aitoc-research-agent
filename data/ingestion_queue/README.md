# Ingestion Queue

Use this folder for sources that need to be fetched, parsed, or converted into evidence notes.

The queue exists because the agent should not rely on memory when tools are unavailable. If a source cannot be fetched now, record it here with the required retrieval method and claim type.

